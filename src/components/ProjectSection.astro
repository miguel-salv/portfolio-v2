---
interface Props { id: string; address: string; title: string; }
const { id, address, title } = Astro.props;
---
<article class="writeup-section" id={id} data-address={address} data-reveal>
  <h2>{title}</h2>
  <slot />
</article>
